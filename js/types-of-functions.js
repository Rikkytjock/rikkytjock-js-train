
// Peforming a tast
function greet(name, lastName) {
    console.log('Hello' + name + '' + lastName);
}

// Calculating a value
function square (number) {
    return number * number;
}

console.log(square(2));

// Types of functions are bascily a set of statements that either peforms a task or calculates a value.